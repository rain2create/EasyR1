# EasyR1 Worker 架构详解

> 本文档解释 Worker、三合一架构、以及与分布式的关系。

---

## 一、核心概念

### 1.1 Worker 是什么？

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Worker 的定义                                   │
└─────────────────────────────────────────────────────────────────────────┘

Worker 不是"一张卡"，而是一个"进程"，管理一组 GPU！  可以理解为一台机器就是一个worker 这里是一台机器(8张gpu)

一个 Worker:
  - 是一个 Ray Actor（分布式进程）
  - 管理一组 GPU（比如 8 卡）
  - 包含多个模型组件（Actor、Rollout、Ref）
  - 内部通过 FSDP/TP 做分布式计算
```

woker的存在主要是为了Ray分布式调度(跨机器！！！通信，多台机器同时启动进程) 以及 方便显存管理 （传统要存三个模型，有了worker之后可只存俩个，省显存）！！！



### 1.2 三合一是什么？

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    ActorRolloutRef 三合一架构                            │
└─────────────────────────────────────────────────────────────────────────┘

一个 Worker 内部包含三个组件：

┌─────────────────────────────────────────────────────────────────────────┐
│                           FSDPWorker                                    │
│                     (一个进程，管理 8 个 GPU)                             │
│                                                                         │
│   ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
│   │     Actor       │  │    Rollout      │  │   Ref Policy    │        │
│   │   (FSDP 训练)   │  │  (vLLM 推理)    │  │  (FSDP 参考)    │        │
│   │                 │  │                 │  │                 │        │
│   │ - 权重分片存储   │  │ - Tensor Paral │  │ - 权重分片存储   │        │
│   │ - 计算梯度      │  │ - 生成 response │  │ - 计算 KL 散度  │        │
│   │ - 参数更新      │  │                 │  │                 │        │
│   └─────────────────┘  └─────────────────┘  └─────────────────┘        │
│                                                                         │
│   共享同一组 GPU (8 卡)，通过显存复用节省资源                              │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、代码中的体现

### 2.1 Worker 初始化

```python
# verl/workers/fsdp_workers.py:71-92
class FSDPWorker(Worker):
    def __init__(self, config, role):
        # 判断 Worker 包含哪些组件
        self._has_actor = role in ["actor", "actor_rollout", "actor_rollout_ref"]
        self._has_critic = role == "critic"
        self._has_rollout = role in ["rollout", "actor_rollout", "actor_rollout_ref"]
        self._has_ref = role in ["ref", "actor_rollout_ref"]
```

### 2.2 三合一初始化流程

```python
# verl/workers/fsdp_workers.py:408-422 (init_model)
if self._has_actor:
    # 1. 创建 FSDP Actor (训练用)
    self.actor = DataParallelPPOActor(...)
    
if self._has_rollout:
    # 2. 创建 vLLM Rollout (推理用)
    self.rollout = vLLMRollout(tensor_parallel_size=...)
    
if self._has_ref:
    # 3. 创建 Ref Policy (KL 散度计算用)
    self.ref_policy = DataParallelPPOActor(...)
```

---

## 三、分布式架构详解

### 3.1 单机 8 卡场景

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    单机 8 卡，1 个 Worker                                │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────────────────┐
                    │         Driver (Ray 主进程)          │
                    │   - 数据分发                         │
                    │   - GRPO 组内标准化                  │
                    │   - 收集指标                         │
                    └─────────────────────────────────────┘
                                     │
                                     │ RPC 调用
                                     ▼
        ┌─────────────────────────────────────────────────────────────┐
        │                    FSDPWorker (1 个进程)                     │
        │                                                             │
        │  GPU 0      GPU 1      GPU 2      GPU 3      ...   GPU 7   │
        │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐        ┌──────┐   │
        │  │shard │  │shard │  │shard │  │shard │        │shard │   │
        │  │  0   │  │  1   │  │  2   │  │  3   │        │  7   │   │
        │  └──────┘  └──────┘  └──────┘  └──────┘        └──────┘   │
        │                                                             │
        │  内部通信: NCCL (All-Gather, Reduce-Scatter, All-Reduce)   │
        └─────────────────────────────────────────────────────────────┘


为什么是 1 个 Worker？
───────────────────────────────────────────────────────────────────────────
- 单机 8 卡通过 NCCL 直接通信，不需要跨进程
- 1 个 Worker 管理 8 个 GPU，内部用 FSDP 分片
- 显存复用：Actor、Rollout、Ref 共享 GPU，不同时使用
```

### 3.2 双机 16 卡场景

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    双机 16 卡，2 个 Worker                               │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────────────────┐
                    │         Driver (Ray 主进程)          │
                    └─────────────────────────────────────┘
                                     │
                    ┌────────────────┴────────────────┐
                    │                                 │
                    ▼                                 ▼
        ┌─────────────────────────┐     ┌─────────────────────────┐
        │   FSDPWorker 0          │     │   FSDPWorker 1          │
        │   (机器 0, GPU 0-7)     │     │   (机器 1, GPU 8-15)    │
        │                         │     │                         │
        │   GPU 0-7:             │     │   GPU 8-15:            │
        │   Actor shard 0-7      │     │   Actor shard 8-15     │
        │   Rollout (TP=2)       │     │   Rollout (TP=2)       │
        │   Ref shard 0-7        │     │   Ref shard 8-15       │
        └─────────────────────────┘     └─────────────────────────┘
                    │                                 │
                    └───────────── NCCL ─────────────┘
                              跨机器通信


关键点:
───────────────────────────────────────────────────────────────────────────
- 每个 Worker 管理一台机器的 8 卡
- FSDP 跨 Worker 分片：16 卡共同存储完整模型
- 每个 Worker 内部有独立的 Rollout (vLLM)
```

---

## 四、Worker 内部如何协同工作？

### 4.1 时分复用显存

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    三合一组件的时分复用                                   │
└─────────────────────────────────────────────────────────────────────────┘

时间轴:
───────────────────────────────────────────────────────────────────────────

T1: Rollout 阶段 (生成 response)
    ┌─────────────────────────────────────────────────────────────┐
    │  vLLM Rollout 使用显存                                       │
    │  - 加载权重 (从 FSDP 同步)                                   │
    │  - KV Cache                                                 │
    │  - 生成 responses                                           │
    │                                                             │
    │  Actor/Ref: 休眠状态 (权重 offload 到 CPU 或释放)            │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
T2: 训练阶段 (计算梯度、更新)
    ┌─────────────────────────────────────────────────────────────┐
    │  FSDP Actor 使用显存                                         │
    │  - All-Gather 收集权重                                       │
    │  - 前向/反向计算                                             │
    │  - Reduce-Scatter 同步梯度                                   │
    │                                                             │
    │  Rollout: sleep() 释放显存                                   │
    │  Ref: 需要时加载 (计算 KL 散度)                               │
    └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
T3: 下一轮 Rollout...
    回到 T1

显存复用效果:
───────────────────────────────────────────────────────────────────────────
- 不需要同时放下 Actor + Rollout + Ref 三份权重
- 只需要 max(Actor显存, Rollout显存, Ref显存)
- 节省约 2/3 显存！  只需要存一份显存就够，但其实actor和ref都要存
```

### 4.2 权重同步流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Actor → Rollout 权重同步                              │
└─────────────────────────────────────────────────────────────────────────┘

代码位置: verl/workers/sharding_manager/fsdp_vllm.py

Step 1: FSDP Actor 权重分片存储
───────────────────────────────────────────────────────────────────────────
  GPU 0: W_shard_0
  GPU 1: W_shard_1
  ...
  GPU 7: W_shard_7

Step 2: All-Gather 收集完整权重
───────────────────────────────────────────────────────────────────────────
  每个 GPU 都得到完整权重 W = [W_shard_0, ..., W_shard_7]
  
  代码: actor_weights = get_model_state_dict(self.module)

Step 3: 按 TP 切分，加载到 vLLM
───────────────────────────────────────────────────────────────────────────
  如果 TP=2:
    GPU 0,1: 各存 W 的一半 (左/右)
    GPU 2,3: 各存 W 的一半
    ...
  
  代码: model.load_weights(actor_weights)

Step 4: vLLM 推理
───────────────────────────────────────────────────────────────────────────
  Tensor Parallel 并行计算

Step 5: vLLM sleep，释放显存给 Actor 训练
───────────────────────────────────────────────────────────────────────────
  代码: self.inference_engine.sleep(level=1)
```

---

## 五、完整架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    EasyR1 完整分布式架构                                  │
└─────────────────────────────────────────────────────────────────────────┘

                           ┌─────────────────┐
                           │    Driver       │
                           │   (CPU 进程)    │
                           │                 │
                           │ - 数据加载      │
                           │ - GRPO 标准化   │
                           │ - 日志记录      │
                           └─────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
         ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
         │  FSDPWorker 0   │ │  FSDPWorker 1   │ │  FSDPWorker N   │
         │  (机器 0)       │ │  (机器 1)       │ │  (机器 N)       │
         │                 │ │                 │ │                 │
         │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │
         │  │   Actor   │  │ │  │   Actor   │  │ │  │   Actor   │  │
         │  │  (FSDP)   │  │ │  │  (FSDP)   │  │ │  │  (FSDP)   │  │
         │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │
         │                 │ │                 │ │                 │
         │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │
         │  │  Rollout  │  │ │  │  Rollout  │  │ │  │  Rollout  │  │
         │  │  (vLLM)   │  │ │  │  (vLLM)   │  │ │  │  (vLLM)   │  │
         │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │
         │                 │ │                 │ │                 │
         │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │
         │  │   Ref     │  │ │  │   Ref     │  │ │  │   Ref     │  │
         │  │  (FSDP)   │  │ │  │  (FSDP)   │  │ │  │  (FSDP)   │  │
         │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │
         │                 │ │                 │ │                 │
         │  GPU 0-7        │ │  GPU 8-15       │ │  GPU ...        │
         └─────────────────┘ └─────────────────┘ └─────────────────┘
                    │               │               │
                    └───────────────┴───────────────┘
                              NCCL 通信


关键理解:
───────────────────────────────────────────────────────────────────────────
1. Driver: 单进程，负责任务调度和数据分发
2. Worker: 每台机器一个，管理本机所有 GPU
3. 三合一: Actor + Rollout + Ref 共享 GPU，时分复用
4. FSDP: 跨 Worker 分片，所有 GPU 共同存储完整模型
5. Rollout: 每个 Worker 内独立，用 TP 做推理
```

---

## 六、回答你的问题

### Q1: "一个 Worker 是一张卡吗？"

**不是！** Worker 是一个进程，管理一组 GPU（通常是一台机器的所有 GPU）。

- 单机 8 卡 → 1 个 Worker，管理 8 个 GPU
- 双机 16 卡 → 2 个 Worker，每个管理 8 个 GPU

### Q2: "三合一是什么意思？"

一个 Worker 内部同时包含三个组件：
1. **Actor** (FSDP): 用于训练，计算梯度，更新参数
2. **Rollout** (vLLM): 用于推理，生成 response
3. **Ref Policy** (FSDP): 参考模型，计算 KL 散度

它们**时分复用**同一组 GPU，不是同时运行，而是轮流使用显存。

### Q3: "Worker 内部的分布式是怎样的？"

```
Worker 内部:
───────────────────────────────────────────────────────────────────────────
- FSDP Actor: 所有 GPU 分片存储权重 (world_size = 8)
- vLLM Rollout: 按 TP 分组，每组内并行推理 (TP = 2, dp = 4)
- FSDP Ref: 同 Actor，分片存储

跨 Worker:
───────────────────────────────────────────────────────────────────────────
- FSDP: 所有 GPU 共同分片 (world_size = 16)
- 每个 Worker 内的 Rollout 独立处理不同数据
```

### Q4: "vLLM 是单独的吗？"

是的！vLLM Rollout 是 Worker 内的一个**独立组件**：
- 有自己的推理引擎
- 权重从 FSDP Actor 同步过来
- 用 Tensor Parallel 做推理
- 用完后 sleep 释放显存

---

## 七、配置示例

```yaml
# examples/config.yaml
trainer:
  nnodes: 1           # 1 台机器
  n_gpus_per_node: 8  # 每台机器 8 卡
  # 总共 1 个 Worker，管理 8 个 GPU

worker:
  rollout:
    tensor_parallel_size: 2  # vLLM 用 TP=2，分 4 组 dp
```

对应架构：

```
1 个 Worker, 8 个 GPU
├── Actor: FSDP 分片 (8 卡)
├── Rollout: TP=2 (4 组 dp × 2 卡 TP)
└── Ref: FSDP 分片 (8 卡)
```